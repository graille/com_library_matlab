function mex_source_finder(directory_path)
    % TODO
    current_path = pwd;
    
    raw_files = dir(directory_path);
    files = {};
    
    for i = 1:length(raw_files)
        if 
    end
    
end